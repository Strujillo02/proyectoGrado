services:
  proyectoapi:
    build: ProyectoDeGrado
    mem_limit: 720m
    ports:
      - "8080:8080"
    environment:
      DB_URL: ${DB_URL}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}
    restart: always
